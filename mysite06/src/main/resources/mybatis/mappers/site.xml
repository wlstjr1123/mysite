<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="site">	
	<select id="find" parameterType="boolean" resultType="sitevo">
		<![CDATA[
		select no, title, welome, profile, description from site
		]]>
	</select>
	
	<update id="update" parameterType="sitevo">
		<![CDATA[
		update site set title=#{title }, welome=#{welcome }, profile=#{profile }, description=#{description }
		]]>
	</update>

</mapper>